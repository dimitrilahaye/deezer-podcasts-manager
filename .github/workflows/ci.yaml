name: 🎧 Deezer Podcasts Manager » CI

on:
  pull_request:
    branches: [ main ]

jobs:
  checks:
    name: 💎 Quality Checks
    runs-on: ubuntu-latest

    steps:
    - name: ↪️ Checkout repository
      uses: actions/checkout@v4

    - name: 💚 Use Node
      uses: actions/setup-node@v4
      with:
        node-version: "20"

    - name: 📦 Install
      run: npm install

    - name: 💄 Lint
      run: npm run lint

    - name: 🧪 Tests
      run: npm run test
